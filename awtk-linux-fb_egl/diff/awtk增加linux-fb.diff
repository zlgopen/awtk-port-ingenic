 3rd/glad/glad.c                   | 14 ++++++++------
 src/SConscript                    |  4 ++++
 src/vgcanvas/vgcanvas_nanovg_gl.c |  2 --
 3 files changed, 12 insertions(+), 8 deletions(-)

diff --git a/3rd/glad/glad.c b/3rd/glad/glad.c
index f23fb29c..7640f646 100644
--- a/3rd/glad/glad.c
+++ b/3rd/glad/glad.c
@@ -70,20 +70,23 @@ int open_gl(void) {
         "/System/Library/Frameworks/OpenGL.framework/Versions/Current/OpenGL"
     };
 #else
-    static const char *NAMES[] = {"libGL.so.1", "libGL.so"};
+    static const char *NAMES[] = {"libGL.so.1", "libGL.so", "libGLESv2.so"};
 #endif
 
     unsigned int index = 0;
     for(index = 0; index < (sizeof(NAMES) / sizeof(NAMES[0])); index++) {
         libGL = dlopen(NAMES[index], RTLD_NOW | RTLD_GLOBAL);
-
         if(libGL != NULL) {
 #ifdef __APPLE__
             return 1;
 #else
-            gladGetProcAddressPtr = (PFNGLXGETPROCADDRESSPROC_PRIVATE)dlsym(libGL,
-                "glXGetProcAddressARB");
-            return gladGetProcAddressPtr != NULL;
+			if (index < 2) {
+				gladGetProcAddressPtr = (PFNGLXGETPROCADDRESSPROC_PRIVATE)dlsym(libGL,
+					"glXGetProcAddressARB");
+				return gladGetProcAddressPtr != NULL;
+			} else {
+				return 1;
+			}
 #endif
         }
     }
@@ -117,7 +120,6 @@ void* get_proc(const char *namez) {
         result = dlsym(libGL, namez);
 #endif
     }
-
     return result;
 }
 
diff --git a/src/SConscript b/src/SConscript
index 6f9f31e2..4b56c065 100644
--- a/src/SConscript
+++ b/src/SConscript
@@ -25,6 +25,8 @@ sources=Glob('layouters/*.c') + \
 
 if NATIVE_WINDOW == 'sdl':
   sources += Glob('native_window/native_window_sdl.c')
+elif NATIVE_WINDOW == 'fb_gl':
+  sources += Glob('native_window/native_window_fb_gl.c')
 else:
   sources += Glob('native_window/native_window_raw.c')
 
@@ -36,6 +38,8 @@ elif os.environ['LCD'] == 'SDL_FB':
   sources += ['lcd/lcd_sdl2.c', 'main_loop/main_loop_sdl.c']
 elif os.environ['LCD'] == 'SDL_FB_MONO':
   sources += ['lcd/lcd_sdl2_mono.c', 'main_loop/main_loop_sdl.c']
+elif os.environ['LCD'] == 'FB_GL':
+  sources += ['lcd/lcd_nanovg.c']
 
 sources += [
   'input_methods/input_method_creator.c'
diff --git a/src/vgcanvas/vgcanvas_nanovg_gl.c b/src/vgcanvas/vgcanvas_nanovg_gl.c
index c2575c43..bae02543 100644
--- a/src/vgcanvas/vgcanvas_nanovg_gl.c
+++ b/src/vgcanvas/vgcanvas_nanovg_gl.c
@@ -34,8 +34,6 @@
 #ifndef WITHOUT_GLAD
 #include "glad/glad.h"
 #endif /*WITHOUT_GLAD*/
-#include <SDL_opengl.h>
-#include <SDL_opengl_glext.h>
 
 #include "nanovg.h"
 #include "tkc/utf8.h"
